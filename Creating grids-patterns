As the course progressed, we learnt if statements, for loops, arrays etc.. Initially we created a grid using if statements, for loops and also 10prints using if statements. Then we covered arrays on the next day. Using arrays and for loops we first created a grid then made an array of four colors and used that array to assign colors to every grid randomly. Another function, "random" was used to do this. After understanding this concept, we made patterns using images, for that we ha dto design 4 images in a way that each image will match with all the other three if placed along any sides. I made, common points on each sides, 2 on each to acheieve this. After that I uploaded every images into the code and insteaded of making grids using rectangles, I made grids using images which change randomly after constant intervals. Then I used mouseClicked function so that thegrid changes on every mouse click. After that I tried changing only that particular grid I clicked on, the using "tint" function, I changed the color of the grid when clicked on.
